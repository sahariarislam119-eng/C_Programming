#include<stdio.h>
int main(){
    float p=10000;  // P is Principle
    float r=5;    // R is rate
    float t=4;    // T is time
    float si;
    si = (p*r*t)/100;
    printf("Simple Interst is :%f",si);
    return 0;
}